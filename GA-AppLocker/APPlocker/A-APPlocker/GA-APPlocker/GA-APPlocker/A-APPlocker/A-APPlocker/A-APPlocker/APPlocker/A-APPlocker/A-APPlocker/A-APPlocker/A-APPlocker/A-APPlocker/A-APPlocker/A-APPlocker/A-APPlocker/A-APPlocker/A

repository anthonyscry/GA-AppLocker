# Update-RulesGrid null guard

# FIXED: Added null guard at line 60
# OLD CODE (had empty catch):
#     $rulesDataGrid.ItemsSource = $null
# NEW CODE:
if ($null -eq $Rules) {
    Write-AppLockerLog -Message "Update-RulesGrid: Rules is null, skipping update" -Level Warning
    return
}

# This prevents "Cannot index into a null array" error
